# Математическая модель игры «Воздушный шар»

## 1. Генерация точки краха

Классическая схема crash-игр с экспоненциальным распределением и house edge:

```
u ~ Uniform(0, 1)
E = -ln(1 - u)            // Exp(1), E[E] = 1
crashRaw = (1 + E/α) · edge
crash = clamp(crashRaw, min_crash_multiplier, max_multiplier)
```

Если `crashRaw > max_multiplier` — считается мгновенный крах на старте
(вероятность при max=50 и α=1.2 пренебрежимо мала, но обработана).

Параметры (config): `alpha = 1.2`, `edge = 0.95`, `min_crash_multiplier = 1.0`,
`max_multiplier = 50`.

**Обоснование.** Для экспоненциальной модели:
- медиана: `crash_med = (1 + ln2/α)·edge = (1 + 0.693/1.2)·0.95 ≈ 1.60`
- P(crash < 2) = P(E < α·(2/edge − 1)) = 1 − e^(−1.2·1.155) ≈ 0.75
- P(crash ≥ 5) = e^(−1.2·(5/0.95−1)) = e^(−5.11) ≈ 0.006
- матожидание коэффициента с edge: E[crash] = edge·(1 + 1/α) = 0.95·1.833 ≈ 1.74 < 2,
  при ставке «держать до краха» EV = E[crash]·P(дожить) < 1 — игра в минус
  для игрока без cashout-стратегии, что и требуется оператору.

То есть большинство раундов короткие (вовлечение + сжигание бонусов),
редкие длинные (азарт «а вдоль долетит»), матожидание контролируется edge.

## 2. Рост коэффициента

```
m(t) = 1 + growth · t^p
growth = multiplier_growth_rate = 0.005,  p = growth_curve = 1.8
```

Степенная кривая с p > 1 даёт плавный старт (производная в нуле равна нулю —
шар «набирает высоту» не сразу) и разгон к середине полёта. Стартовую
скорость и «распределение темпа» можно крутить параметром growth_curve:
p = 1 — линейный рост, p > 1 — медленнее в начале, быстрее потом.

Дискретизация: округление до 0.01 на каждом шаге (и на сервере, и на клиенте).

Примеры: m(5с)=×1.09, m(10с)=×1.32, m(20с)=×2.10, m(30с)=×3.04
(приблизительно, до дискретизации). Достижение уровня i: `m ≥ 1 + i·level_step`,
`level_step = 1.0` → уровень 1 при ×2, уровень 12 при ×13; дальше — продолжение по коэффициентам (×14, ×15…, без очков). Шар достигает верха экрана при `balloon_top_at = 5` и дальше летит без набора высоты — меняется только фон.

`fps = 60`, `delta = 1/60` — клиент рендерит rAF, сервер считает по
монотонному `System.nanoTime`, поэтому подстройка по сети не требуется:
клиент лишь синхронизируется polling'ом (250 мс) и при расхождении > 0.15
прижимается к серверному значению. Cashout всегда пересчитывается сервером
от своего времени — клиентские часы результат не определяют.

## 3. Позиция бустера

До раунда сервер выбирает уровень из дискретного распределения:

```
line ∈ 1..L,  P(line = i) = line_loot_prob[i] / Σ line_loot_prob
```

Сумма не обязана равняться 1 — нормализуется. Вероятности убывают с ростом
уровня: чаще бустер рядом с низом (частая маленькая радость), реже наверху
(редкий большой сюрприз). RED: 12 уровней, GREEN: 9.

При `passed ≥ boosterLevel` до cashout: `m_eff = m · boosterValue`,
+`points_xN_bonus` очков. После cashout бустер не активируется.

## 4. Начисление очков и выигрыш

```
points = passed·points_per_line
       + (cashout ? points_cashout_bonus : 0)
       + (бустер активирован ? points_xN_bonus : 0)
win    = bet × m_eff(t_cashout)          // при cashout, округление вниз
```

По умолчанию: 10 за уровень, +25 за cashout, +20 за бустер. Очки начисляются
и при проигрыше (за пройденные уровни) — стимул «долетать хотя бы до уровня».

## 5. Provably fair (упрощённый, как требует ТЗ для MVP)

```
seed = 16 случайных байт (SecureRandom)
hash = SHA-256(seed | roundId | crashPoint с 2 знаками)
```

`hash` публикуется до полёта, `seed` раскрывается после краха. Клиент может
пересчитать хеш и убедиться, что точка краха была зафиксирована заранее.
Клиенту crashPoint до взрыва не передаётся — досрочный cashout по известному
краху невозможен.

## 6. Режим разработчика

`"dev_seed": "любая строка"` — генератор краха становится детерминированным:
`u = SHA-256(dev_seed | roundId)` → первые 8 байт как дробь. Удобно для
отладки сценариев (например, задать `min_crash_multiplier: 20`, чтобы гарантированно
долетать до высоких уровней и тестировать бустеры).
